package oa.gg;
/**
 * 
给一个class叫 Job, 里面有 startTime, duration, cpu_usage.
写一个function input是list of Jobs, 还有一个integer 代表最大cpu usage,
返回 boolean 看这些job 是否使用了超过limit的cpu.
similar to meeting roomII
 */
public class CPUUsage {
    
}
